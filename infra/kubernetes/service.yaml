apiVersion: v1
kind: Service
metadata:
  name: cst-multimoedas-frontend-service
  namespace: cst-multimoedas
spec:
  type: NodePort
  selector:
    app: cst-multimoedas-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
