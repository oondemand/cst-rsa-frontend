apiVersion: apps/v1
kind: Deployment
metadata:
  name: cst-rsa-homolog
  namespace: cst-multimoedas-homolog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cst-rsa-homolog
  template:
    metadata:
      labels:
        app: cst-rsa-homolog
    spec:
      containers:
        - name: cst-rsa-homolog-container
          image: ghcr.io/oondemand/cst-rsa:${IMAGE_TAG}-homolog
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "64Mi"
              cpu: "0.05"
            limits:
              memory: "128Mi"
              cpu: "0.1"
