apiVersion: apps/v1
kind: Deployment
metadata:
  name: cst-multimoedas-frontend-homolog
  namespace: cst-multimoedas-homolog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cst-multimoedas-frontend-homolog
  template:
    metadata:
      labels:
        app: cst-multimoedas-frontend-homolog
    spec:
      containers:
        - name: cst-multimoedas-frontend-homolog-container
          image: ghcr.io/oondemand/cst-multimoedas-frontend:${IMAGE_TAG}-homolog
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "64Mi"
              cpu: "0.05"
            limits:
              memory: "128Mi"
              cpu: "0.1"
