apiVersion: apps/v1
kind: Deployment
metadata:
  name: cst-multimoedas-frontend
  namespace: cst-multimoedas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cst-multimoedas-frontend
  template:
    metadata:
      labels:
        app: cst-multimoedas-frontend
    spec:
      containers:
        - name: cst-multimoedas-frontend-container
          image: ghcr.io/oondemand/cst-multimoedas-frontend:${IMAGE_TAG}-prod
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "64Mi"
              cpu: "0.05"
            limits:
              memory: "128Mi"
              cpu: "0.1"
