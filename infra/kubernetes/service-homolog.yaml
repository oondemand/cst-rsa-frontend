apiVersion: v1
kind: Service
metadata:
  name: cst-rsa-frontend-homolog-service
  namespace: cst-rsa-homolog
spec:
  type: NodePort
  selector:
    app: cst-rsa-frontend-homolog
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
