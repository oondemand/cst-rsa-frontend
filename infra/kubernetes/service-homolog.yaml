apiVersion: v1
kind: Service
metadata:
  name: cst-multimoedas-frontend-homolog-service
  namespace: cst-multimoedas-homolog
spec:
  type: NodePort
  selector:
    app: cst-multimoedas-frontend-homolog
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
